# 工具说明书

## 视频导入

用途：把某个目录里的视频导入网页视频播放器。

```bash
rtk npm run import:videos -- "视频所在目录"
```

支持：
- `.mp4`
- `.webm`

导入时会自动完成：
- 扫描指定目录第一层的视频文件
- 复制到 `public/videos/<id>/original.mp4` 或 `original.webm`
- 生成 `public/videos/<id>/thumb.png`
- 追加 `src/data/videos.ts` 视频配置
- 校验视频资源是否完整

示例：

```bash
rtk npm run import:videos -- "C:\Users\ADMIN\Videos\待导入"
```

注意：
- 重复导入同一个自动生成 `id` 的视频会跳过。
- 缩略图从视频约 40% 位置自动抽帧。
- 视频标题默认使用文件名。

## 视频资源校验

用途：检查 `src/data/videos.ts` 中配置的视频、封面、缩略图是否存在。

```bash
rtk npm run validate:videos
```

## 构建检查

用途：检查 TypeScript 和前端生产构建是否通过。

```bash
rtk npm run build
```

## 本地开发

用途：启动 Vite 开发服务器。

```bash
rtk npm run dev
```

## 本地预览

用途：预览生产构建结果。

```bash
rtk npm run preview
```
